name: OnePlus_PadPro_Official_Pure_Build

on:
  workflow_dispatch:
    inputs:
      custom_command:
        description: '编译指令'
        required: true
        default: 'python3 kernel_platform/build_with_bazel.py -t pineapple gki'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 空间清理
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc "/usr/local/share/boost" "$AGENT_TOOLSDIRECTORY"
          df -h

      - name: 2. 检出配置
        uses: actions/checkout@v4

      - name: 3. 同步源码 (保持清单原样)
        run: |
          mkdir -p workspace && cd workspace
          repo init -u https://github.com/${{ github.repository }}.git -b main -m manifest.xml --depth=1
          repo sync -c -j$(nproc) --no-tags --no-clone-bundle --force-sync

      - name: 4. 激活官方源码中的隐藏优化
        run: |
          # 进入官方源码的标准路径 (GKI 6.1+ 路径)
          cd workspace/kernel_platform/common/arch/arm64/configs
          DEFCONFIG="gki_defconfig"
          
          echo "🛠️ 正在官方配置中开启网络与内存增强..."

          # 开启官方 BBRv3 / BBR 支持
          # 只要官方源码里有 tcp_bbr.c，这些配置就会生效
          cat >> $DEFCONFIG <<EOF
          CONFIG_TCP_CONG_BBR=y
          CONFIG_TCP_CONG_BBRV3=y
          CONFIG_NET_SCH_FQ=y
          CONFIG_NET_SCH_FQ_CODEL=y
          CONFIG_TCP_CONG_ADVANCED=y
          CONFIG_TCP_CONG_WESTWOOD=y
          EOF

          # 开启官方 MGLRU 内存管理
          cat >> $DEFCONFIG <<EOF
          CONFIG_LRU_GEN=y
          CONFIG_LRU_GEN_ENABLED=y
          EOF

          # 开启高性能调度与 1000Hz
          cat >> $DEFCONFIG <<EOF
          CONFIG_HZ_1000=y
          CONFIG_SCHED_WALT=y
          EOF
          
          # 禁用官方调试日志 (减负)
          sed -i 's/CONFIG_DEBUG_KERNEL=y/CONFIG_DEBUG_KERNEL=n/' $DEFCONFIG
          
          echo "✅ 官方配置修改完毕"

      - name: 5. 赋予官方脚本执行权限
        run: |
          cd workspace
          find kernel_platform -name "*.sh" -exec chmod +x {} +
          find kernel_platform -name "bazel" -exec chmod +x {} +
          [ -f kernel_platform/build_with_bazel.py ] && chmod +x kernel_platform/build_with_bazel.py

      - name: 6. 运行官方 Bazel 编译
        run: |
          cd workspace
          ${{ github.event.inputs.custom_command }}

      - name: 7. 打包刷机包
        if: success()
        run: |
          KERNEL_IMAGE=$(find workspace/out -name "Image.gz" | head -n 1)
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git ak3
          cp "$KERNEL_IMAGE" ak3/
          cd ak3
          sed -i 's/device.name1=.*/device.name1=OnePlusPadPro/' anykernel.sh
          zip -r9 ../OnePlus_PadPro_Kernel_Official_Optimized.zip *
          
      - name: 8. 上传成品
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Official_Optimized_Kernel
          path: "*.zip"